# -----------------------------------------------------------------------------
# Deriverse Trading Analytics Dashboard – environment template
# Copy to .env or .env.local and fill in values.
# -----------------------------------------------------------------------------

# Database (required)
# Postgres connection string. Used for sync storage, metrics, and annotations.
# Examples: Neon, Supabase, Railway, or any Postgres with SSL.
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# -----------------------------------------------------------------------------
# Deriverse program (optional – app has defaults)
# Only transactions that invoke these program IDs are treated as Deriverse.
# Use PROGRAM_ID for a single ID, or PROGRAM_IDS for multiple (comma-separated).
# -----------------------------------------------------------------------------
# PROGRAM_ID=CDESjex4EDBKLwx9ZPzVbjiHEHatasb5fhSJZMzNfvw2
PROGRAM_IDS=CDESjex4EDBKLwx9ZPzVbjiHEHatasb5fhSJZMzNfvw2,Drvrseg8AQLP8B96DBGmHRjFGviFNYTkHueY9g3k27Gu

# Protocol version (optional, default 6). For future version-specific decoding.
VERSION=6

# -----------------------------------------------------------------------------
# Solana RPC (optional – defaults to devnet)
# Must return full tx data including meta.logMessages for trade derivation.
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.devnet.solana.com
